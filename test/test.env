# Loaded by `npm test` via node --env-file. Test-only values; the database is
# the throwaway container from `npm run test:db:up`.
BOT_TOKEN=123456:TEST_TOKEN_NOT_A_REAL_BOT
CHAT_ID=-1001234567890
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=55432
POSTGRES_USER=havuja
POSTGRES_PASSWORD=testpass
POSTGRES_DATABASE=havujatest
# Skip the wizard's post-confirmation pause; it is pure UI polish.
CLEANUP_DELAY_MS=1
# ALICE is an admin, BOB is not.
ADMIN_USER_IDS=42
